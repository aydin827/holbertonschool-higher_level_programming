#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    for row in matrix:
        new_matrix[]
        for num in row:
            new_row.append(num * num)
        new_matirx.append(new_row)
    return new_matrix
